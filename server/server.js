const express = require('express');
const mongo = require('mongojs')

const url_model = require('url_model.js');
const app = express();

const chars = ''

app.post('/lenkr', ( ) => {

});



app.listen(3000, ( ) => {
	console.log('Express @:3000');
});